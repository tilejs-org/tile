---
title: Database
description: Crie e configure sua instância principal do Tile.JS Database
icon: Database
---

import { TypeTable } from "fumadocs-ui/components/type-table";
import { File, Folder, Files } from "fumadocs-ui/components/files";
import { DatabaseIcon, TableProperties } from "lucide-react";

# Database

A classe `Database` é a fachada principal da lib.

Ela é responsável por:

- configurar o adapter padrão local
- aceitar adapters customizados
- criar e cachear collections
- delegar `connect()` e `disconnect()` quando o adapter suporta lifecycle

---

## Criando uma Database local

A forma mais simples de uso continua sendo o adapter padrão local.

```ts title="src/index.ts"
import { Database } from "@tile.js/database";

const database = new Database();
```

Por padrão, isso cria uma database chamada `test`, armazenada em:

```text
.tile/database/test
```

---

## Configuração completa do modo padrão

```ts title="src/index.ts"
import { Database } from "@tile.js/database";

const database = new Database({
  dbName: "my-database",
  storage: "workspace",
  compression: false,
  logger: {
    enabled: true,
    colors: true,
  },
});
```

---

## Usando um adapter customizado

`storage` também pode receber um `StorageAdapter`.

```ts title="src/index.ts"
import { Database } from "@tile.js/database";
import { mongodbAdapter } from "@tile.js/database/adapters/mongodb";
import { MongoClient } from "mongodb";

const client = new MongoClient("mongodb://127.0.0.1:27017");

const database = new Database({
  storage: mongodbAdapter(client, {
    dbName: "my-database",
  }),
});
```

<Callout type="warning">
Quando `storage` é um adapter customizado, `dbName` e `compression` deixam de ser válidos no `new Database(...)`.

Essas opções devem ser configuradas dentro do próprio adapter.
</Callout>

---

## Opções

<TypeTable
  type={{
    dbName: {
      description: "Nome da database usada pelo adapter padrão da lib.",
      type: "string",
      default: '"test"',
    },
    storage: {
      description: "Pode ser `workspace`, `global` ou um `StorageAdapter` customizado.",
      type: '"workspace" | "global" | StorageAdapter',
      default: '"workspace"',
    },
    logger: {
      description: "Configuração do logger.",
      type: "LoggerConfig",
      default: "{ enabled: false, colors: false }",
    },
    compression: {
      description: "Compacta BSON com Gzip no adapter padrão.",
      type: "boolean",
      default: "false",
    },
  }}
/>

---

## Modos do adapter padrão

### Workspace

Salva os dados dentro do diretório atual do projeto.

```text
.tile/
└── database/
    └── test/
```

Ideal para aplicações, APIs e projetos que mantêm seus dados junto do código-fonte.

---

### Global

Salva os dados no diretório do usuário.

```text
~/.tile/
└── database/
    └── test/
```

Útil para CLIs e ferramentas usadas em vários projetos.

---

## Lifecycle

A classe `Database` expõe três métodos importantes:

```ts
await database.connect();
console.log(database.isConnected()); // true
await database.disconnect();
```

No adapter padrão, `disconnect()` força `flush()` antes de encerrar.

Em adapters remotos, `connect()` e `disconnect()` delegam para o adapter quando o lifecycle é gerenciado por ele.

---

## Collections e cache

A mesma collection não é recriada duas vezes.

```ts
const usersA = database.collection("users", userSchema);
const usersB = database.model("users", userSchema);

console.log(usersA === usersB); // true
```

Você também pode recuperar collections já registradas:

```ts
const users = database.getCollection("users");
const usersAlias = database.getModel("users");
```

---

## Estrutura criada pelo adapter padrão

<Files>
  <Folder name=".tile">
    <Folder name="database" defaultOpen>
      <Folder name="my-database" defaultOpen />
    </Folder>
  </Folder>
</Files>

Você não precisa criar nenhuma dessas pastas manualmente.

---

## Responsabilidades da Database

<Cards cols={2}>
  <Card icon={<DatabaseIcon />} title="Collections">
    Todas as collections pertencem a uma instância de `Database`.
  </Card>

  <Card icon={<DatabaseIcon />} title="Adapter">
    Centraliza a configuração do backend de persistência.
  </Card>

  <Card icon={<DatabaseIcon />} title="Logger">
    Mantém toda a configuração de logs em um único lugar.
  </Card>

  <Card icon={<DatabaseIcon />} title="Lifecycle">
    Delegação de `connect()` e `disconnect()` para o backend quando suportado.
  </Card>
</Cards>

---

## Próximo passo

Agora que a instância principal foi criada, o próximo passo é escolher o backend e depois definir o schema das collections.

<Cards>
  <Card icon={<DatabaseIcon />} title="Adapters" href="/docs/database/adapters">
    Entenda o adapter padrão, MongoDB e Mongoose.
  </Card>

  <Card icon={<TableProperties />} title="Schema" href="/docs/database/schema">
    Aprenda a definir tipos, defaults, índices e validações.
  </Card>
</Cards>
